version: '3.8'

services:
  backend:
    build:
      dockerfile: Dockerfile
      context: ./backend
    ports:
      - "8080:8080"
    environment:
      JWT_SECRET: "${JWT_SECRET:?JWT_SECRET debe estar definida}"
